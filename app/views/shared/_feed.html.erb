<% if @feed_items.any? %>
  <ol class="posts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items %>
<% else %>
  <p>
    表示する投稿がありません。<br>
    新たに投稿をするか、他のユーザーをフォローしてみましょう。
  </p>
<% end %>