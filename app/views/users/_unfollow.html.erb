<%= form_with model: current_user.active_relationships.find_by(followed_id: @user.id),
              method: :delete do |form| %>
  <%= form.submit "フォローをやめる", class:"btn unfollow-btn", data:{ confirm:"フォローをやめてよろしいですか？" } %>
<% end %>