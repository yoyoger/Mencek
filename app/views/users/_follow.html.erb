<%= form_with model: current_user.active_relationships.build, url: relationships_url, local: false do |form| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= form.submit "フォローする", class:"btn follow-btn responsive-off" %>
  <%= form.submit "+", class:"btn follow-btn responsive-on" %>
<% end %>